{
  "name": "restamio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "build:web": "node scripts/build-web.mjs",
    "build:web:owner": "node scripts/build-web.mjs --channel=owner",
    "android:sync": "npm run build:web && cap sync android",
    "android:debug": "npm run android:sync && cd android && ./gradlew assembleProductionDebug",
    "android:owner": "npm run build:web:owner && cap sync android && cd android && ./gradlew assembleOwnerDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@napi-rs/canvas": "^1.0.8"
  }
}
